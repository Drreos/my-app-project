# 🚀 Быстрый старт: ИИ-Ассистент

## За 5 минут до запуска

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Получите API ключ OpenAI

👉 https://platform.openai.com/api-keys

Создайте новый ключ и скопируйте его.

### 3. Добавьте ключ в .env

Откройте ваш `.env` файл и добавьте:

```env
# ИИ настройки
AI_ENABLED=true
OPENAI_API_KEY=sk-ваш-ключ-здесь
AI_MODEL=gpt-4o-mini
AI_AUTO_RESPOND=true
```

### 4. Запустите бота

```bash
python main.py
```

## ✅ Готово!

Теперь ИИ автоматически отвечает клиентам:

- 🤖 **Невидимо** - клиенты не знают что это ИИ
- ⚡ **Мгновенно** - ответ за 1-3 секунды
- 🎯 **Умно** - использует вашу базу знаний FAQ
- 👨‍💼 **Передает оператору** - когда нужна помощь человека

## Как проверить что работает?

1. Создайте тестовый тикет
2. Напишите вопрос (например: "Как пополнить баланс?")
3. ИИ должен ответить за 1-3 секунды
4. Проверьте логи: `tail -f bot.log | grep "AI"`

## Стоимость

С моделью `gpt-4o-mini`:
- 1000 сообщений ≈ $0.10-0.30
- 10000 сообщений ≈ $1-3

**Экономия:** ИИ обрабатывает 70-80% вопросов за копейки вместо зарплаты оператора!

## Настройка

Все настройки в `.env`:

```env
AI_ENABLED=true              # Включить/выключить ИИ
AI_AUTO_RESPOND=true         # Автоответы (true) или вручную (false)
AI_MODEL=gpt-4o-mini        # Модель (mini = быстро и дешево)
AI_MAX_TOKENS=1000          # Длина ответа
AI_TEMPERATURE=0.7          # Креативность (0.0-1.0)
```

## Что дальше?

📖 Полная документация: `README_AI.md`

---

**Вопросы?** Проверьте логи: `tail -f bot.log | grep "AI"`

