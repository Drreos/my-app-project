# 🤖 Интеграция ИИ-Ассистента в Telegram бот поддержки

## Описание

ИИ-ассистент автоматически отвечает на вопросы клиентов **невидимо для них**. Клиенты не знают, что общаются с ИИ - они получают мгновенные ответы, которые выглядят как обычные сообщения поддержки.

## Как это работает

1. **Клиент создает тикет** → ИИ мгновенно отвечает на основе базы знаний FAQ
2. **Клиент задает дополнительные вопросы** → ИИ продолжает отвечать до тех пор, пока не ответит оператор
3. **Оператор отвечает** → ИИ перестает отвечать, управление передается человеку
4. **Клиент ничего не знает** → Для него это просто быстрая и эффективная поддержка

## Преимущества

✅ **Мгновенные ответы** - клиенты не ждут часами  
✅ **Невидимость** - клиенты не знают про ИИ  
✅ **Снижение нагрузки** - ИИ отвечает на типовые вопросы  
✅ **Плавная передача** - когда нужно, оператор подключается  
✅ **База знаний** - ИИ использует ваш FAQ для точных ответов

## Установка и настройка

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Получите OpenAI API ключ

1. Зарегистрируйтесь на https://platform.openai.com/
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Скопируйте его

### 3. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и настройте:

```env
# ===== ОБЯЗАТЕЛЬНЫЕ НАСТРОЙКИ =====

# API ключ OpenAI (обязательно!)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Включить ИИ (true/false)
AI_ENABLED=true

# ===== ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ =====

# Модель ИИ (рекомендуется gpt-4o-mini для скорости и экономии)
AI_MODEL=gpt-4o-mini

# Максимум токенов в ответе (500-2000)
AI_MAX_TOKENS=1000

# Температура (0.0-1.0, где 0.7 - оптимально)
AI_TEMPERATURE=0.7

# Автоматически отвечать (true) или только вручную (false)
AI_AUTO_RESPOND=true
```

### 4. Обновите базу данных

При первом запуске бота база данных автоматически обновится с новыми полями для отслеживания ИИ.

```bash
python main.py
```

## Настройка базы знаний

ИИ использует ваш FAQ из `config.py`. Чтобы улучшить ответы ИИ:

1. Откройте `config.py`
2. Найдите раздел `FAQ_QUESTIONS`
3. Добавьте/обновите вопросы и ответы для каждой темы
4. Перезапустите бота

Пример:

```python
FAQ_QUESTIONS = {
    "balance": {
        "ru": {
            "question1": "Как пополнить баланс?",
            "answer1": "Подробная инструкция по пополнению..."
        }
    }
}
```

## Мониторинг и логи

### Просмотр логов ИИ

```bash
tail -f bot.log | grep "AI"
```

Примеры логов:

- `AI response sent to user 123456` - ИИ успешно ответил
- `Human already responded to user 123456, skipping AI` - Оператор уже ответил
- `AI auto-response disabled` - ИИ отключен в настройках

### Проверка работы ИИ

1. Создайте тестовый тикет от имени клиента
2. Проверьте логи - вы должны увидеть "AI response sent"
3. Клиент должен получить ответ в течение 1-3 секунд
4. Ответьте от имени оператора
5. ИИ больше не будет отвечать этому клиенту

## Управление ИИ

### Отключить ИИ временно

В `.env`:
```env
AI_ENABLED=false
```

### Отключить автоответы (только по запросу оператора)

В `.env`:
```env
AI_AUTO_RESPOND=false
```

### Изменить модель ИИ

Доступные модели:
- `gpt-4o-mini` - быстрая, дешевая (рекомендуется)
- `gpt-4o` - более мощная, но дороже
- `gpt-3.5-turbo` - самая дешевая, но менее точная

## Стоимость использования

Примерная стоимость с моделью `gpt-4o-mini`:

- 1000 сообщений клиентов ≈ $0.10 - $0.30
- 10,000 сообщений клиентов ≈ $1 - $3

**Экономия:** Один оператор стоит ~$500-1000/месяц, а ИИ обрабатывает 70-80% вопросов за ~$10-30/месяц

## Безопасность

✅ API ключ хранится в `.env` и не попадает в git  
✅ ИИ не имеет доступа к личным данным клиентов  
✅ ИИ не может совершать действия от имени оператора  
✅ Все ответы ИИ логируются для контроля

## Устранение неполадок

### ИИ не отвечает

1. Проверьте `.env`: `AI_ENABLED=true` и `AI_AUTO_RESPOND=true`
2. Проверьте API ключ OpenAI
3. Проверьте логи: `tail -f bot.log | grep "AI"`
4. Проверьте баланс на https://platform.openai.com/account/usage

### ИИ отвечает неправильно

1. Обновите FAQ в `config.py`
2. Увеличьте `AI_MAX_TOKENS` для более подробных ответов
3. Измените `AI_TEMPERATURE` (меньше = точнее, больше = креативнее)
4. Смените модель на `gpt-4o` для лучшего качества

### ИИ отвечает даже когда ответил оператор

Проверьте что в `database.py` функция `update_ticket_support_activity` устанавливает `human_responded = TRUE`

## Поддержка

Если возникли вопросы или проблемы:

1. Проверьте логи бота
2. Проверьте настройки в `.env`
3. Убедитесь что API ключ OpenAI действителен
4. Проверьте баланс на OpenAI

## Дополнительные возможности

### Анализ тональности

ИИ может анализировать настроение клиента (позитивное/негативное/нейтральное). Для использования:

```python
from ai_assistant import ai_assistant

sentiment = await ai_assistant.analyze_sentiment(user_message, lang="ru")
# Вернет: "positive", "neutral" или "negative"
```

### Определение эскалации

ИИ автоматически определяет когда нужно передать вопрос оператору:

```python
should_escalate = ai_assistant.should_escalate_to_human(ai_response)
```

Ключевые слова для эскалации: "оператор", "специалист", "не могу помочь" и т.д.

---

**Готово!** ИИ-ассистент интегрирован и готов помогать вашим клиентам 24/7! 🚀

