# 📋 Шпаргалка: ИИ-Ассистент

## 🚀 Быстрый запуск (3 команды)

```bash
# 1. Установить зависимости
pip install -r requirements.txt

# 2. Добавить в .env
echo "AI_ENABLED=true" >> .env
echo "OPENAI_API_KEY=sk-ваш-ключ" >> .env
echo "AI_MODEL=gpt-4o-mini" >> .env
echo "AI_AUTO_RESPOND=true" >> .env

# 3. Запустить
python main.py
```

## 🔑 Получить API ключ OpenAI

https://platform.openai.com/api-keys

## 📊 Проверить работу

```bash
# Смотреть логи ИИ
tail -f bot.log | grep "AI"

# Проверить баланс OpenAI
https://platform.openai.com/account/usage
```

## ⚙️ Настройки в .env

```env
AI_ENABLED=true              # Вкл/выкл ИИ
AI_AUTO_RESPOND=true         # Автоответы
AI_MODEL=gpt-4o-mini        # Модель
AI_MAX_TOKENS=1000          # Длина ответа
AI_TEMPERATURE=0.7          # Креативность (0-1)
```

## 💰 Стоимость (gpt-4o-mini)

| Сообщений | Стоимость |
|-----------|-----------|
| 100       | ~$0.03    |
| 1,000     | ~$0.30    |
| 10,000    | ~$3       |
| 100,000   | ~$30      |

## 🎯 Как работает

1. Клиент пишет → ИИ отвечает (1-3 сек)
2. Клиент не знает что это ИИ
3. Оператор отвечает → ИИ отключается
4. Клиент доволен быстрым ответом

## 🛠️ Команды

```bash
# Смотреть все логи
tail -f bot.log

# Только ИИ логи
tail -f bot.log | grep "AI"

# Перезапустить бота
pkill -f main.py && python main.py

# Проверить зависимости
pip list | grep openai
```

## 📝 Обновить базу знаний

1. Открыть `config.py`
2. Найти `FAQ_QUESTIONS`
3. Добавить вопросы/ответы
4. Перезапустить бота

## ❌ Проблемы?

### ИИ не отвечает
```bash
# Проверить настройки
cat .env | grep AI

# Проверить логи
tail -f bot.log | grep "AI"
```

### ИИ отвечает неправильно
- Обновить FAQ в `config.py`
- Увеличить `AI_MAX_TOKENS`
- Сменить модель на `gpt-4o`

### Нет денег на OpenAI
- Пополнить: https://platform.openai.com/account/billing

## 📚 Документация

- `QUICK_START_AI.md` - Быстрый старт
- `README_AI.md` - Полная документация
- `AI_INTEGRATION_SUMMARY.md` - Что сделано

## 🎊 Готово!

ИИ работает 24/7, клиенты довольны, операторы свободны! 🚀

