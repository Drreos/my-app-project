# 🔒 Автоматическое закрытие тикетов

## 📋 Описание

Система автоматически закрывает тикеты, если **клиент не отвечает более 1 часа** после ответа оператора.

**Важно:** Клиент **НЕ получает уведомления** о закрытии - тикет закрывается тихо.

---

## ⚙️ Как работает:

### Условия автозакрытия:

1. ✅ **Оператор ответил клиенту**
2. ✅ **Клиент не ответил в течение N часов** (по умолчанию 1 час)
3. ✅ **Тикет имеет статус "open"**

### Что происходит при автозакрытии:

```
1. Система проверяет тикеты каждые 5 минут
   ↓
2. Находит тикеты где:
   - Последнее сообщение от оператора
   - Прошло > 1 час без ответа клиента
   ↓
3. ТИХО закрывает тикет:
   - Обновляет статус в БД → "closed"
   - НЕ отправляет сообщение клиенту ← ВАЖНО!
   - Отправляет уведомление в чат поддержки
   - Закрывает тему форума
```

---

## 🎯 Примеры работы:

### Сценарий 1: Нормальное закрытие

```
14:00 - Клиент: "Как пополнить баланс?"
14:01 - ИИ: "Пополнение через Telegram Stars..."
14:02 - Клиент: "А как вывести?"
14:03 - Оператор: "Вывод через раздел Подарки"
        ↓ Клиент не отвечает ↓
15:03 - Система: 🔒 Тикет автоматически закрыт
        - Клиент НЕ получает уведомление
        - В чате поддержки: "Тикет закрыт (неактивность 1 час)"
```

### Сценарий 2: Клиент ответил вовремя

```
14:00 - Оператор: "Попробуйте обновить приложение"
14:30 - Клиент: "Спасибо, помогло!"
        ↓
Автозакрытие НЕ срабатывает (клиент ответил) ✅
```

### Сценарий 3: Клиент создает новый тикет

```
14:00 - Тикет #1 закрыт автоматически
15:00 - Клиент: /start → создает новый тикет #2
        ↓
Все работает нормально, новый тикет открывается ✅
```

---

## 📊 Что видят участники:

### Клиент видит:

```
❌ НИЧЕГО не видит!
```

Тикет просто закрывается тихо. Если клиент попытается написать в старый тикет - бот автоматически создаст новый.

---

### Оператор видит в чате поддержки:

```
┌─────────────────────────────────────────┐
│ 🎫 Тикет #52 - 💰 Balance              │
├─────────────────────────────────────────┤
│ [Клиент]                                │
│ Как пополнить?                          │
│                                         │
│ [Оператор]                              │
│ Через Telegram Stars                    │
│                                         │
│ 🔒 Тикет автоматически закрыт:         │
│ клиент не отвечал более 1 час(а/ов).   │
│ Если потребуется, клиент может         │
│ создать новый тикет.                    │
│                                         │
│ [Тема форума закрыта] ✅                │
└─────────────────────────────────────────┘
```

---

## 🔧 Настройки:

### В `.env` файле:

```bash
# Автозакрытие тикетов
AUTO_CLOSE_ENABLED=true         # Включить/выключить функцию
AUTO_CLOSE_HOURS=1              # Через сколько часов закрывать (по умолчанию 1)
```

### Примеры настройки:

```bash
# Закрывать через 30 минут (агрессивно)
AUTO_CLOSE_HOURS=0.5

# Закрывать через 1 час (по умолчанию)
AUTO_CLOSE_HOURS=1

# Закрывать через 2 часа (мягко)
AUTO_CLOSE_HOURS=2

# Отключить автозакрытие
AUTO_CLOSE_ENABLED=false
```

---

## 📝 Логика в коде:

### 1. Проверка условий (`main.py`):

```python
if (
    AUTO_CLOSE_ENABLED
    and thread_id
    and last_support  # Поддержка ответила
    and (not last_client or last_support > last_client)  # Последнее сообщение от поддержки
    and now - last_support >= auto_close_delta  # Прошло N часов
):
    # Автозакрытие
```

### 2. Процесс закрытия:

```python
# 1. Закрываем тикет в БД
await auto_close_ticket(user_id)

# 2. НЕ уведомляем клиента (тихое закрытие!)

# 3. Уведомляем чат поддержки
await bot.send_message(
    chat_id=SUPPORT_CHAT_ID,
    text=f"🔒 Тикет автоматически закрыт: клиент не отвечал более {AUTO_CLOSE_HOURS} час(а/ов)",
    message_thread_id=thread_id
)

# 4. Закрываем тему форума
await bot.close_forum_topic(
    chat_id=SUPPORT_CHAT_ID,
    message_thread_id=thread_id
)
```

---

## 🧪 Как протестировать:

### Быстрый тест (1 минута вместо 1 часа):

```bash
# 1. В .env установите:
AUTO_CLOSE_HOURS=0.016  # 1 минута (1/60 часа)

# 2. Перезапустите бот:
docker-compose restart bot

# 3. Создайте тикет и получите ответ от оператора

# 4. Подождите 1 минуту

# 5. В логах увидите:
# 🔒 Ticket auto-closed for user 698471795 due to inactivity
# ✅ Ticket auto-closed successfully for user 698471795

# 6. В чате поддержки появится сообщение о закрытии
```

### Нормальный тест (1 час):

```bash
# 1. В .env:
AUTO_CLOSE_HOURS=1

# 2. Создайте тикет
# 3. Оператор отвечает
# 4. Клиент молчит 1 час
# 5. Тикет автоматически закроется
```

---

## 📊 Мониторинг:

### Смотрите логи:

```bash
docker-compose logs -f bot | grep -E "🔒|auto-close"
```

**Что искать:**
```
⚙️  Auto-close enabled: True, timeout: 1 hour(s)  ← При запуске
🔒 Ticket auto-closed for user 698471795 due to inactivity  ← Закрытие
✅ Ticket auto-closed successfully for user 698471795  ← Успех
```

---

## ⚠️ Важные детали:

### 1. Клиент НЕ узнает о закрытии

❌ **НЕ отправляется:**
```
"Ваш тикет закрыт из-за неактивности"
```

✅ **Вместо этого:**
- Тикет просто закрывается
- Клиент ничего не получает
- Если напишет - создастся новый тикет

---

### 2. Только после ответа оператора

Условие:
```python
last_support > last_client  # Последнее сообщение от оператора
```

**Это значит:**
- ❌ НЕ закроется если клиент написал, а оператор не ответил
- ✅ Закроется только если оператор ответил, а клиент не отреагировал

---

### 3. Работает параллельно с другими напоминаниями

```
- Напоминание оператору (60 мин без ответа)
- Напоминание о закрытии (8 часов)
- АВТОЗАКРЫТИЕ (1 час без ответа клиента) ← НОВОЕ
```

Все три механизма работают независимо!

---

## 🎯 Зачем это нужно:

### Проблема:

```
- Клиент задал вопрос
- Оператор ответил
- Клиент исчез
- Тикет висит открытым днями/неделями
- Захламляет чат поддержки
```

### Решение:

```
✅ Автоматически закрываем "мертвые" тикеты
✅ Клиент не знает о закрытии (не мешаем ему)
✅ Оператор видит что тикет закрыт автоматически
✅ Если клиент вернется - просто создаст новый тикет
```

---

## 🚀 Готово!

```bash
# Запуск
docker-compose up -d

# Проверка настроек
docker-compose logs bot | grep "Auto-close"

# Должны увидеть:
# ⚙️  Auto-close enabled: True, timeout: 1 hour(s)
```

**🎉 ТИКЕТЫ БУДУТ АВТОМАТИЧЕСКИ ЗАКРЫВАТЬСЯ ТИХО И НЕЗАМЕТНО ДЛЯ КЛИЕНТА! 🎉**

